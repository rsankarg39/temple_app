org.gradle.jvmargs=-Xmx1G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.parallel=false
kotlin.incremental=false
android.useAndroidX=true

# Use a JDK version supported by the Android Gradle/Kotlin toolchain.
# Java 26 is not compatible with the current Gradle/Kotlin setup in this project.
# Set this path to a JDK 17 or 21 installation on your machine.
org.gradle.java.home=C:/Program Files/Android/Android Studio1/jbr
# Opt out of the AGP 9+ new DSL to keep compatibility with the Flutter Gradle plugin
android.newDsl=false
